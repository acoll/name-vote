import React from "react";
import styled from "styled-components";

const StyledAddButton = styled.div`
  color: tomato;
  // text-transform: uppercase;

`;

export default function AddButton() {
  return <StyledAddButton>Add New</StyledAddButton>;
}
